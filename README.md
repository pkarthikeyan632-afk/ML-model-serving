# ML-model-serving
This project demonstrates a simple and structured approach to serving machine learning models using FastAPI. It provides a REST API that can be extended to host trained ML models and expose prediction endpoints for real-world applications.
